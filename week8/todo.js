console.log("Howdy");

const todoList = [];

todoList.push("Sweep the Cieling");

todoList.push("Mow the carpet");

todoList.push("Cook the dishes");

console.log (todoList);

todoList.pop();

console.log (todoList);

for (let i = 0; i < todoList.length; i++) {
    console.log[i];
    console.log(todoList[i]);
}